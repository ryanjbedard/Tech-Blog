<section class="container">
  <h3 class="blog-name">{{blog.name}}</h3>
  <h4>{{post_title}} {{format_time gallery.post_date}}</h4>
  <section class="flex-row justify-center">
    {{#each blog.posts as |post|}}
    {{> post-details}}
    {{/each}}
  </section>
</section>